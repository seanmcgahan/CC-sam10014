# week3
Wanted to try making something with the p5 polar library, but I am a little confused on how to use it. I believe I have to download it off of the github page, but not sure how I can get it so that I can use it in my sketeches. Not sure which file to download and how i get it working in my current p5js library. Would be curious to learn how to do this.

However, from just reading through the github page, I can see how this would open up a lot more options (and use way less keystrokes as well!) than brute forcing the positions of these shapes. For one, brute forcing it would be a little difficult to get the mathematically correct positioning and angle of each shape. Looking at the examples and their posted code, they take only a few lines of code to get some very complex layouts. Even compared to a for loop, its still much simpler. It also seems much more predictable than a for loop as well. Keeping for loops in check and having them do what I want them to do I have found to be a little challenging. By just having one function while using the polar library, this simplifies things greatly for those specific types of patterns. 

My sketches didn't use any layouts similar to the ones that the polar library can make (mine shapes were generally in horizonal rows and vertical collumns), so using this would have created something completely different looking than anything that I made.





## Getting Started

Open `index.html` in your web browser and start editing `sketch.js`.

## Running Locally

For projects with media files, use a local server:

```bash
# Using Python
python -m http.server 8000

# Using Node.js
npx http-server

# Using VS Code Live Server extension
# Right-click index.html -> "Open with Live Server"
```

## Resources

- [p5.js 2.0](https://beta.p5js.org/)
- [p5.js Reference](https://p5js.org/reference/)
